import React from 'react'

import moment from 'moment'

// props.project.features.map(function(feature) {
//   return {
//     id: feature.id,
//     name: feature.name,
//     start: moment(feature.estimateStart).format('YYYY-MM-DD'),
//     end: moment(feature.estimateEnd).format('YYYY-MM-DD')
//   }
// })

function ProjectGantt(props) {
  return <p>GANTT</p>
}

export default ProjectGantt
